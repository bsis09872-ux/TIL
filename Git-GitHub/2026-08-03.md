# Git & GitHub 기초

> 📅 2026.08.03

---

# 📚 오늘 배운 내용

오늘은 Git과 GitHub의 기본 개념, Git 설치 및 초기 설정, Git의 버전 관리 방식, Git의 작업 흐름과 기본 명령어를 학습했다.

---

# 1. 개발 환경 설치

Git을 사용하기 위해 다음 프로그램들을 설치하였다.

- Git
- GitHub
- Visual Studio Code
- Notion
- Slack
- Google 계정

### Git 사용자 정보 등록

```bash
git config --global user.name "사용자명"
git config --global user.email "이메일"
```

### VS Code를 기본 에디터로 설정

```bash
git config --global core.editor "code --wait"
```

---

# 2. DOS(Command Prompt) 기본 명령어

| 명령어 | 설명 |
|--------|------|
| cls | 터미널 화면 지우기 |
| cd | 디렉터리(폴더) 이동 |
| dir | 파일 및 폴더 목록 확인 |
| dir /w | 목록을 가로 형태로 출력 |
| type | 파일 내용 확인 |

---

# 3. Git Config

Git 설정은 적용 범위에 따라 세 가지로 나뉜다.

| 옵션 | 적용 범위 |
|------|-----------|
| --local | 현재 저장소(Repository)에만 적용 |
| --global | 현재 사용자에게 적용 |
| --system | 컴퓨터의 모든 사용자에게 적용 |

설정이 중복될 경우 우선순위는 다음과 같다.

```
Local > Global > System
```

즉, Local 설정이 있으면 Global이나 System 설정보다 우선 적용된다.

---

# 4. SSH Key 생성

SSH는 GitHub와 **비밀번호를 반복해서 입력하지 않고 안전하게 통신하기 위한 인증 방식**이다.

### SSH Key 생성

```bash
ssh-keygen -t ed25519 -C "이메일"
```

### 공개 키 확인

Windows

```bash
type id_ed25519.pub
```

Mac

```bash
cat ~/.ssh/id_ed25519.pub
```

확인한 공개 키를 GitHub의

**Settings → SSH and GPG Keys → New SSH Key**

에 등록한다.

---

# 5. Git이란?

Git은 **분산 버전 관리 시스템(Distributed Version Control System, DVCS)** 이다.

프로젝트의 변경 이력을 기록하고 관리하며, 필요할 때 이전 버전으로 되돌아갈 수 있도록 도와준다.

기존 버전 관리 시스템은 변경된 부분(Diff)을 중심으로 관리했지만, Git은 프로젝트의 전체 상태를 하나의 **Snapshot(스냅샷)** 으로 기록한다.

다만 동일한 파일은 내부적으로 중복 저장하지 않아 저장 공간도 효율적으로 관리한다.

나는 Git을 게임의 **Save Point**와 비슷하다고 이해했다.

Commit을 할 때마다 하나의 저장 지점이 생성되고, 필요하면 언제든지 그 시점으로 돌아갈 수 있다.

---

# 6. Git의 작업 흐름

```
Working Directory
        ↓
Staging Area
        ↓
Repository
```

### Working Directory

실제로 파일을 수정하는 작업 공간

### Staging Area

다음 Commit에 포함할 변경 사항을 선택하는 공간

### Repository

Commit을 통해 버전이 저장되는 공간

### 저장소 생성

```bash
git init
```

### Staging Area에 올리기

```bash
git add 파일명
```

현재 디렉터리 이하의 모든 변경 사항 추가

```bash
git add .
```

### Commit

```bash
git commit -m "메시지"
```

Staging Area에 있는 변경 사항을 하나의 버전으로 저장한다.

### Commit 기록 확인

```bash
git log
```

---

# 7. 이전 버전으로 이동

특정 Commit 상태 확인

```bash
git checkout <commit id>
```

직전 위치로 이동

```bash
git checkout -
```

---

# 8. 자주 사용하는 명령어

### Staging Area의 변경 사항 확인

```bash
git diff --staged
```

Commit 전에 어떤 내용이 저장될지 확인할 수 있다.

### Commit 기록 한 줄로 보기

```bash
git log --oneline
```

### Branch를 포함한 전체 기록 보기

```bash
git log --graph --decorate --all
```

---

# 💡 오늘 이해한 내용

오늘 가장 중요하게 이해한 것은 Git의 작업 흐름이다.

파일을 수정했다고 바로 버전이 저장되는 것이 아니라,

```
Working Directory
        ↓
Staging Area
        ↓
Repository
```

의 과정을 거쳐야 하나의 버전(Commit)으로 기록된다.

또한 Commit은 단순히 파일을 저장하는 것이 아니라 프로젝트의 특정 시점을 저장하는 **Save Point**라는 점이 인상적이었다.

---

# 🤔 아직 헷갈리는 부분

- Git과 GitHub의 차이
- Snapshot 방식이 기존 방식보다 왜 효율적인지
- 언제 Local Config를 사용하는 것이 적절한지

---

# 📌 다음에 공부할 내용

- GitHub Repository
- Remote Repository
- git clone
- git push
- git pull
