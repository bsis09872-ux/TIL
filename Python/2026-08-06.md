DATE: 2026-08-06

python & venv 구축

when and how the source code is translated into machine-readable code: compiled languages translate the entire program into machine code before execution, while interpreted languages translate and execute the code line-by-line at runtime.

The kernel is the core component of an operating system that acts as the primary interface between the computer's hardware and its software processes.

python: 변수
- [computer] = 연산 기계
- 계산할 대상 = [변수값]
- 칠판/기록할 곳 = [메모리]
- 메모리는 여러 개의 방으로 구성됨
- 이름표; 방 번호/주소/; 참조 = [변수]
  - "값을 가진 공간의 주소를 가진 이름표"
 -> 변수 = 값

변수명 = 값
- 여기서 등호(=)는 대입 연산자이다.
- 참조가 끊기면 메모리의 값은 쓰레기가 됨. 이걸 GC(가비지 콜렉터; 청소부)가 지켜보다가 제거해줌. 메모리 여유 공간이 있어야 돌아가기 때문에 필요한 것. (과거에는 할당 변수명이 달라져서 참조가 끊기면 개발자가 직접 안쓰는 메모리를 제거했어야 했음. 현재는 알아서 제거됨)
- 
  
